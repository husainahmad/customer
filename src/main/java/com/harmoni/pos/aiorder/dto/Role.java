package com.harmoni.pos.aiorder.dto;

/** Chat participants in an order conversation. */
public enum Role {
    /** The human customer. */
    USER,
    /** The AI ordering assistant. */
    ASSISTANT
}