<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.harmoni.pos.customer.adapter.out.persistence.mybatis.CustomerMapper">

    <sql id="baseColumns">
        id, name, phone, email, deleted_at, created_at, updated_at
    </sql>

    <resultMap id="customerMap" type="com.harmoni.pos.customer.adapter.out.persistence.mybatis.entity.CustomerEntity">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="deletedAt" column="deleted_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="customerMap">
        SELECT <include refid="baseColumns"/>
        FROM customers
        WHERE id = #{id}
          AND deleted_at IS NULL
    </select>

    <!-- Includes soft-deleted rows on purpose: the unique key on phone still
         reserves the phone of a deleted customer. -->
    <select id="findByPhoneIncludingDeleted" resultMap="customerMap">
        SELECT <include refid="baseColumns"/>
        FROM customers
        WHERE phone = #{phone}
    </select>

    <select id="search" resultMap="customerMap">
        SELECT <include refid="baseColumns"/>
        FROM customers
        WHERE deleted_at IS NULL
        <if test="search != null">
            AND (
                name LIKE CONCAT('%', #{search}, '%')
                OR phone LIKE CONCAT('%', #{search}, '%')
                OR email LIKE CONCAT('%', #{search}, '%')
            )
        </if>
        ORDER BY created_at DESC, id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*)
        FROM customers
        WHERE deleted_at IS NULL
        <if test="search != null">
            AND (
                name LIKE CONCAT('%', #{search}, '%')
                OR phone LIKE CONCAT('%', #{search}, '%')
                OR email LIKE CONCAT('%', #{search}, '%')
            )
        </if>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO customers (name, phone, email)
        VALUES (#{name}, #{phone}, #{email})
    </insert>

    <update id="update">
        UPDATE customers
        SET name = #{name},
            phone = #{phone},
            email = #{email},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND deleted_at IS NULL
    </update>

    <update id="deleteById">
        UPDATE customers
        SET deleted_at = CURRENT_TIMESTAMP,
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND deleted_at IS NULL
    </update>

</mapper>
